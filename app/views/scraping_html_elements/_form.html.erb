<%= form_with(model: scraping_html_element, local: true) do |form| %>
  <% if scraping_html_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scraping_html_element.errors.count, "error") %> prohibited this scraping_html_element from being saved:</h2>

      <ul>
      <% scraping_html_element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :target_element %>
    <%= form.text_field :target_element %>
  </div>

  <div class="field">
    <%= form.label :scraping_page_id %>
    <%= form.text_field :scraping_page_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
