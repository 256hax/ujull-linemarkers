<%= form_with(model: scraping_page, local: true) do |form| %>
  <% if scraping_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scraping_page.errors.count, "error") %> prohibited this scraping_page from being saved:</h2>

      <ul>
      <% scraping_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :page_url %>
    <%= form.text_field :page_url %>
  </div>

  <div class="field">
    <%= form.label :scraping_latest_date %>
    <%= form.text_field :scraping_latest_date %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
